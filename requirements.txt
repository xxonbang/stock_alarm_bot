# Stock Insight Bot 의존성 패키지

# 주가 데이터 수집
# 1.0 이상 버전 사용, 2.0 Breaking Change 방지
yfinance>=1.0.0,<2.0.0

# 한국 주식 수급 데이터 (KRX 공식 데이터 기반)
# 외국인/기관 순매매량 수집에 사용
pykrx>=1.0.0

# Google AI (Gemini) - 최신 v2 SDK
google-genai>=1.0.0

# 텔레그램 봇 (curl_cffi로 교체하여 TLS Fingerprint 차단 우회)
curl_cffi>=0.7.0

# 웹 크롤링
beautifulsoup4>=4.11.1

# 설정 파일 파싱
PyYAML>=6.0.1
python-dotenv>=1.0.0

# FRED API (연준 데이터)
fredapi>=0.5.1

# RSS 피드 파싱
feedparser>=6.0.10

# 번역 라이브러리 (Gemini API 대신 사용)
deep-translator>=1.11.4

# 로깅 및 유틸리티는 표준 라이브러리 사용

urllib3>=2.0.0
